JSON LD
{{#compare primaryResponse.hits.hits.length ">" 0}}
    Inside Primary Response Compare
    {{#with primaryResponse.hits.hits.[0]._source}}
        Inside Primary Response With
        {{["json-ld"]}}
    {{/with}}
{{else}}
    { "error": "404" }
{{/compare}}
