{
    "query": {
        "bool": {
            "must": [{
                "term": {
                    "organisation.slug": "{{slug}}"
                }
            }]
        }
    },
    "sort": [{
        "Id": {
            "order": "desc"
        }
    }]
}
