{{#if regions ~}}
  {{>elasticsearch_terms field="local_authority_slug" terms=regions}},
{{/if ~}}
{{#if sectors ~}}
  {{>elasticsearch_terms field="sectors_slugs" terms=sectors}},
{{/if ~}}
{{#if roles ~}}
  {{>elasticsearch_terms field="roles_slugs" terms=roles}},
{{/if ~}}
{{#if statuses ~}}
  {{>elasticsearch_terms field="role_status_slug" terms=statuses}},
{{/if}}
{{#ifAll lat lng ~}}
{
  "geo_distance": {
    "distance": "{{default distance "8.04672"}}km",
    "geo_vacancy_coords": {
      "lat": {{lat}},
      "lon": {{lng}}
    }
  }
},
{{/ifAll ~}}
{
  "exists": {
    "field": "title"
  }
}
