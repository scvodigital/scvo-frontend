{{#if data.saved_searches.items.[0] ~}}
  {{~#with data.saved_searches.items.[0] ~}}
    {{~#each (split "sunday,monday,tuesday,wednesday,thursday,friday,saturday" ",")~}}
      {{~#if (contains ../days (add @index 1))~}}
        {{~ this ~}}, 
      {{~/if~}}
    {{~/each~}}
  {{~/with~}}
{{~/if}}
