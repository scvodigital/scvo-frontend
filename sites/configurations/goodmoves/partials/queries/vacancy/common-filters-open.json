{{>queries_vacancy_common_filters @root.request.params.query}},
{{>queries_date_range field="closing_date" gt="now"}},
{{>queries_date_range field="start_at" lt="now"}},
{{>queries_date_range field="end_at" gt="now"}}
