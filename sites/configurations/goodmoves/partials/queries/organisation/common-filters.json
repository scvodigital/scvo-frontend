{{>queries_common_country}},
{
  "terms": {
    "Id": [
      {{join (stringify (map @root.data.organisationsWithVacancies.vacancies.hits.hits (getProperty "_source.organisation.id")))}}
    ]
  }
},
{
  "exists": {
    "field": "title"
  }
}