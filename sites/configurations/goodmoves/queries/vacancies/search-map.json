{
  "size": 1000,
  "_source": [
    "Id",
    "title",
    "organisation.name",
    "organisation.logo",
    "geo_vacancy_coords",
    "active",
    "closing_date",
    "start_at",
    "end_at",
    "home_based",
    "salary",
    "role_status_list",
    "slug"
  ],
  "query": {
    "bool": {
      "filter": [
        {{>queries_vacancy_common_filters request.params.query}},
        {{>queries_vacancy_search_filters request.params.query}}
      ],
      "must": [
        {{>queries_vacancy_search_must request.params.query}}
      ],
      "should": [
        {{>queries_vacancy_search_should request.params.query}}
      ],
      "minimum_should_match": 2
    }
  },
  "sort": {
    "closing_date": "asc"
  }
}
