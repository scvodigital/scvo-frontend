<script src="https://cdnjs.cloudflare.com/ajax/libs/typeahead.js/0.11.1/typeahead.bundle.min.js"></script>
<script>
var terms = {
  sectors: [
    {{#each data.opportunitySupplimentary.types.aggregations.sectors.buckets ~}}
    { field: 'sectors', term: {{{stringify key}}} },
    {{/each ~}}
  ],
  roles: [
    {{#each data.opportunitySupplimentary.types.aggregations.roles.buckets ~}}
    { field: 'roles', term: {{{stringify key}}} },
    {{/each ~}}
  ],
  role_status: [
    {{#each data.opportunitySupplimentary.types.aggregations.role_status.buckets ~}}
    { field: 'role_status', term: {{{stringify key}}} },
    {{/each ~}}
  ]
};
</script>
